# Copy to .env for local testing against Postgres, or set this as an
# environment variable in Render's dashboard for the deployed app.
# Get this from Supabase: Project Settings -> Database -> Connection string (URI).
DATABASE_URL=postgresql://user:password@host:5432/postgres
